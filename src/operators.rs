use crate::types::*;

#[macro_export]
macro_rules! increase_date {
    ($($time_span:expr, TimeSpan), *) => {
        todo!();
    }
}

macro_rules! decrease_date {
    ($($time_span:expr, TimeSpan), *) => {
        todo!();
    }
}